# Build output
[Bb]in/
[Oo]bj/

# Generator's own emitted-source dump (samples/tests opt into writing these via
# EmitCompilerGeneratedFiles + CompilerGeneratedFilesOutputPath for local inspection)
Generated/

# Benchmark run output (reproducible via docs/benchmarks.md's "how to reproduce")
BenchmarkDotNet.Artifacts/

# dotnet pack output (reproducible via `dotnet pack AnvilMap.sln -c Release -o ./nupkg-out`)
*.nupkg
nupkg-out/

# IDE / tooling
.idea/
.vs/
.vscode/
*.user
*.suo

# Claude Code session/config
.claude/

# Internal planning docs - not part of the published/public surface
docs/

# Throwaway/regression investigation harnesses (this project's own tools/ convention) - built and
# run locally, never part of the shipped solution or CI
tools/

# OS cruft
.DS_Store
Thumbs.db
