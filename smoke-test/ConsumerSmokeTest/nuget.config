<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <!-- Populated by `dotnet pack AnvilMap.sln -c Release -o ./nupkg-out` at the repo
         root (see .github/workflows/ci.yml and README.md). -->
    <add key="local-packed" value="../../nupkg-out" />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
